replicaCount: 2

image:
  repository: repo
  tag: latest

service:
  type: NodePort
  port: 80      # service port inside cluster
  nodePort: 30123  # external port on worker nodes

liveColor: blue

colors:
  - blue
  - green

